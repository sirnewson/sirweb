rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write (create, update, delete) to the threads collection
    match /threads/{threadId} {
      allow read, write: if true;
    }
  }
}
